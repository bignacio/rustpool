#[cfg(test)]

mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }

    #[test]
    fn create_poll() {
        assert_eq!(1, 2);
    }

    #[test]
    fn create_tcp_poll() {
        assert_eq!(1, 2);
    }
}
